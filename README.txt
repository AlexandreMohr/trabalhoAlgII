O jogo acompanha um mapa de teste chamado 'mapa.txt'.
Para que a execucao ocorra de forma satisfatoria todos os arquivos devem estar na mesma porta.

para executar o jogo em um terminal de linux execute o comando abaixo:

./jogo mapa.txt
